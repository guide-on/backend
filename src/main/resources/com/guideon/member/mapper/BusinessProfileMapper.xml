<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 실제 사용 시 네임스페이스를 인터페이스 경로로 변경 -->
<mapper namespace="com.guideon.member.mapper.BusinessProfileMapper">
    <select id="existsBizRegNo" parameterType="string" resultType="int">
        SELECT COUNT(1) FROM business_profile WHERE biz_reg_no = #{bizRegNo}
    </select>

    <insert id="insert" parameterType="BusinessProfileVO">
        INSERT INTO business_profile (
            member_id, biz_reg_no, biz_name, open_date, ksic_code,
            business_sgg_code, addr_road, addr_detail,
            created_at, updated_at
        ) VALUES (
                     #{memberId}, #{bizRegNo}, #{bizName}, #{openDate}, #{ksicCode},
                     #{businessSggCode}, #{addrRoad}, #{addrDetail},
                     NOW(), NOW()
                 )
    </insert>
</mapper>